#include<stdio.h>
#include<stdlib.h>
main()
{
	int *ptr, i, n1, n2;
	n1=5;
	ptr=(int*) malloc(n1*sizeof(int));
	for (i=0;i<4;i++)
	{
		scanf("%d",ptr+1);
	}
n2=10; //new size accocation
ptr=realloc(ptr, n2*sizepf(i<M)d)
	
}
